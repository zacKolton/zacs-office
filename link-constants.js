"use strict";

/*
  Change QUOTE_FORM_URL when the Google Form is ready.
*/

const LinkConstants = Object.freeze({
    quoteFormUrl: "#",

    anchors: Object.freeze({
        top: "#top",
        work: "#work",
        quote: "#quote",
        about: "#about",
        faq: "#faq"
    }),

    externalLinks: Object.freeze({
        quoteFormOpensNewTab: true
    })
});